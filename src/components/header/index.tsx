import React from 'react';
import { StyledHeader } from './style';

const Header = () => {
  return (
    <StyledHeader>
      <a href="/">Maple Story</a>
    </StyledHeader>
  );
};

export default Header;
